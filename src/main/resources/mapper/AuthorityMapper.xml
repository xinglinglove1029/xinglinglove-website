<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xingling.mapper.AuthorityMapper" >
  <resultMap id="BaseResultMap" type="com.xingling.model.domain.Authority" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="VARCHAR" />
    <result column="version" property="version" jdbcType="INTEGER" />
    <result column="authority_code" property="authorityCode" jdbcType="VARCHAR" />
    <result column="authority_name" property="authorityName" jdbcType="VARCHAR" />
    <result column="menu_id" property="menuId" jdbcType="VARCHAR" />
    <result column="url" property="url" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="VARCHAR" />
    <result column="creator_id" property="creatorId" jdbcType="VARCHAR" />
    <result column="creator" property="creator" jdbcType="VARCHAR" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="updater_id" property="updaterId" jdbcType="VARCHAR" />
    <result column="updater" property="updater" jdbcType="VARCHAR" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    <result column="del" property="del" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    id, version, authority_code, authority_name, menu_id, url, status, creator_id, creator, create_time,
    updater_id, updater, update_time, del
  </sql>
    <select id="getOwnAuthority" resultMap="BaseResultMap">
        select
            DISTINCT ee.*
        from xl_role_authority aa INNER JOIN xl_role bb on aa.role_id = bb.id
            INNER JOIN xl_user_role cc on cc.role_id = bb.id
            INNER JOIN xl_user dd on dd.id = cc.user_id
            INNER JOIN xl_authority ee on ee.id = aa.authority_id
        where dd.id = #{userId}
    </select>

    <select id="queryListPage" resultType="com.xingling.model.domain.Authority">
        SELECT
        aa.*,bb.menu_name as menuName
        FROM
        xl_authority aa
        LEFT JOIN xl_menu bb on aa.menu_id = bb.id
        <if test="authorityName != null and authorityName != ''">
            AND aa.authority_name LIKE concat('%',#{authorityName},'%')
        </if>
        <if test="authorityCode != null and authorityCode != ''">
            AND aa.authority_code = #{authorityCode}
        </if>
        <if test="status != null and status != ''">
            AND aa.status = #{status}
        </if>
        <if test="menuId != null and menuId != ''">
            AND aa.menu_id = #{menuId}
        </if>
    </select>
</mapper>