<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xingling.mapper.UserRoleMapper" >
  <resultMap id="BaseResultMap" type="com.xingling.model.domain.UserRole" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="role_id" property="roleId" jdbcType="VARCHAR" />
    <id column="user_id" property="userId" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    role_id, user_id
  </sql>
    <select id="queryOwnRoles" resultType="com.xingling.model.dto.RoleDto">
      SELECT
        DISTINCT
          bb.role_code,
          bb.role_name
      FROM
          xl_user_role aa
      LEFT JOIN xl_role bb ON aa.role_id = bb.id
      WHERE aa.user_id = #{userId}
    </select>
    <select id="getBindUserByRoleId" resultType="com.xingling.model.domain.User">
        SELECT
            bb.*
        FROM
            xl_user_role aa
        LEFT JOIN xl_user bb ON aa.user_id = bb.id
        WHERE
            aa.role_id = #{roleId}
        AND user_id != '1'
    </select>
</mapper>